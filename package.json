{
  "name": "pakning-r1",
  "version": "1.0.0",
  "description": "Advanced AI Assistant",
  "main": "index.html",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'No build required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^15.0.0"
  }
}
